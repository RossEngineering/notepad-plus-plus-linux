# Encoding + Large File Fixtures

- `mixed-encoding-cases.json`: byte-level cases (hex encoded) for valid/invalid UTF-8 repair checks.
- `very-large-file-pattern.txt`: seed pattern used by regression tests to synthesize multi-megabyte editing payloads.
