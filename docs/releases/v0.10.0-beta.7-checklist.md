# v0.10.0-beta.7 Release Checklist

> **Linux-only fork notice:** This repository and its releases target Linux only. For the original Windows Notepad++ application, visit [notepad-plus-plus.org](https://notepad-plus-plus.org/).

Use this checklist before creating and publishing the `v0.10.0-beta.7` tag.

## Scope gate

- [x] Scope limited to Arch package metadata compatibility fix.
- [x] No unrelated TODO section scope mixed into this release.
- [x] Prior `Extensions ecosystem` feature scope preserved.

## Quality gate

- [x] `bash -n scripts/release/create_linux_release_artifacts.sh` passes.
- [x] Local archive shape verified to emit `.PKGINFO` without `./` prefix.
- [x] `pacman -Qip` succeeds on archive built with corrected entry layout.

## Packaging gate

- [x] Arch package tar invocation emits explicit entries (`.PKGINFO`, optional `.INSTALL`, `usr`).
- [x] Linux release workflow remains tag-triggered (`.github/workflows/linux-release.yml`).
- [x] Existing native package outputs remain in scope (`.pkg.tar.zst`, `.deb`, `.rpm`).

## Documentation gate

- [x] `docs/releases/v0.10.0-beta.7.md` finalized.
- [x] `docs/releases/v0.10.0-beta.7-checklist.md` finalized.
- [x] `docs/versioning-policy.md` updated with latest development prerelease.
- [x] `TODO.md` updated with maintenance prerelease entry.
- [x] `docs/roadmap.md` updated with beta.7 checklist reference.

## Publish

- [ ] `ga-dev-week1` pushed with final prerelease state.
- [ ] Tag `v0.10.0-beta.7` created and pushed.
- [ ] GitHub prerelease published with matching artifacts.
